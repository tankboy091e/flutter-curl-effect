import 'package:flutter/material.dart';

class BookPortraitView extends StatelessWidget {
  const BookPortraitView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
